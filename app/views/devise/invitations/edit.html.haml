-# %h2= t "devise.invitations.edit.header"
-# = simple_form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :put }) do |f|
  
-#   = render "devise/shared/error_messages", resource: resource
-#   = f.hidden_field :invitation_token
-#   .form-inputs
-#     = f.input :password
-#     = f.input :password_confirmation
-#   .form-actions
-#     = f.button :submit, t("devise.invitations.edit.submit_button")


.flex.min-h-full.flex-col.justify-center.py-12.sm:px-6.lg:px-8
  .sm:mx-auto.sm:w-full.sm:max-w-md
    .mt-6.text-center.text-3xl.tracking-tight.text-gray-900  Set up your account

  .mt-8.sm:mx-auto.mx-3.sm:w-full.sm:max-w-md
    .py-8.px-4.sm:px-10.rounded-md.border.border-neutral-200{class: 'sm:rounded-lg.sm:px-10'}
   
      = simple_form_for resource, as: resource_name, url: invitation_path(resource_name), html: { method: :put } do |f|
       
        = f.hidden_field :invitation_token
        %div.mb-6
          %label.block.text-sm.font-medium.text-gray-700{for: "password"} Password
          .mt-1
   
            = f.input :password, label:false, placeholder: 'Atleast 6 characters and hard to guess', input_html:{ class: 'block w-full input-custom sm:text-sm'}

        %div
          %label.block.text-sm.font-medium.text-gray-700{for: "password_confirmation"} Confirm Password
          .mt-1
      
            = f.input :password_confirmation, required: true, placeholder: 'Confirm Password',label: false, input_html:{class: 'block w-full input-custom sm:text-sm'}

            


        = f.button :submit, value: 'Set Password', class: 'flex w-full  btn-primary mt-5 {type: "submit"}'
