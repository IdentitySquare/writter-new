.mb-5
.flex.flex-col.gap-3
  - posts.includes(:rich_text_draft_body).includes(:rich_text_published_body)&.each do |post|

    - next if post.empty?

    = link_to post do

      = render 'posts/public_card', post: post


