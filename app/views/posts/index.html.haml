.flex.flex-col.mx-auto.max-w-3xl.mt-10.px-5
  
  .text-center.font-serif.text-sm.mb-5
    Your Reading List 
  .text-center.font-semibold.font-serif.text-4xl.mb-5
    Stories & Ideas for Today

  .text-center.font-serif.text-sm.uppercase.mb-5
    #{Date.today.strftime("%A, %d %B")}

  - posts = RecommendationService.new(current_user).recommended_posts(10)
  
  = render 'shared/public_user_posts', posts: posts
  

